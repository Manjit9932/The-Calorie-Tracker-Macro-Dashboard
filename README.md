# The-Calorie-Tracker-Macro-Dashboard
A health-tracking prototype that serves as a user's daily food journal. The application calculates nutritional intake in real time, manages a strict calorie budget, and visually warns the user if they overeat
